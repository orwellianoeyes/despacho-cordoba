# --- Públicas: van al navegador, las protege RLS ---
NEXT_PUBLIC_SUPABASE_URL=https://rtawftdaofurzfcywain.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...

# --- Secreta: SOLO del lado del servidor ---
# En local la toma del .env del repo padre (ver dev.sh).
# En Vercel se carga como Environment Variable del proyecto.
ANTHROPIC_API_KEY=

# --- Secreta: el bot que le escribe a los clientes ---
# Mismo token que usa boletin.py. En local sale del .env del repo padre.
TELEGRAM_BOT_TOKEN=

# --- Secreta: el juicio semántico que decide qué recibe cada cliente ---
# console.typesafe.ai. En local sale del .env del repo padre.
TYPESAFE_API_KEY=
